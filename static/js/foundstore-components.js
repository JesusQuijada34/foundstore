(function attachFoundstoreComponents(window){
  const escapeHtml=value=>String(value??'').replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));
  const packagePath=item=>`/${encodeURIComponent(item.author||item.owner||'')}/${encodeURIComponent(item.slug||'')}`;
  const media=(source,fallback,alt,kind)=>source?`<img src="${escapeHtml(source)}" ${fallback?`data-fallback="${escapeHtml(fallback)}"`:''} alt="${escapeHtml(alt)}" loading="lazy" onerror="if(this.dataset.fallback){this.src=this.dataset.fallback;this.dataset.fallback=''}else{this.hidden=true}">`:'';
  const packageCard=(item,options={})=>{
    const visuals=item.visuals||{},name=item.name||item.slug||'Paquete Fluthin',initial=escapeHtml(name.slice(0,1).toUpperCase()||'?'),author=item.author||item.owner||'No informado',publisher=item.publisher||item.company||author,category=item.category||'Aplicación Fluthin',description=item.description||'Paquete Fluthin validado.',stars=Number.isInteger(item.stars)?`${item.stars} estrellas GitHub`:'Estrellas GitHub no disponibles',targets=(item.platformTargets||[]).map(target=>`<span class="foundstore-package-target">${escapeHtml(target)}</span>`).join(''),extraClass=options.newCard?' new-card':'';
    return `<a class="foundstore-package-card${extraClass}" href="${packagePath(item)}" aria-label="Abrir ficha de ${escapeHtml(name)}"><div class="foundstore-package-main"><div class="foundstore-package-icon">${media(visuals.icon,'',`${name} — icono`,'icon')}<span class="foundstore-package-icon-fallback">${initial}</span></div><div class="foundstore-package-body"><p class="foundstore-package-category">${escapeHtml(category)}</p><h3 class="foundstore-package-name">${escapeHtml(name)}</h3><p class="foundstore-package-summary">${escapeHtml(description)}</p><p class="foundstore-package-fact"><b>Empresa</b>${escapeHtml(publisher)} <span aria-hidden="true">·</span> <b>Creador</b>${escapeHtml(author)}</p><span class="foundstore-package-stars"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="m12 2 3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2Z"/></svg>${escapeHtml(stars)}</span></div></div><div class="foundstore-package-banner">${media(visuals.splash||visuals.banner,visuals.icon,`${name} — vista previa`,'banner')}<span class="foundstore-package-banner-fallback">${initial}</span></div>${targets?`<div class="foundstore-package-targets">${targets}</div>`:''}</a>`;
  };
  const creatorPackageRow=item=>{
    const visuals=item.visuals||{},name=item.name||item.slug||'Paquete Fluthin',initial=escapeHtml(name.slice(0,1).toUpperCase()||'?'),author=item.author||item.owner||'No informado',publisher=item.publisher||item.company||author,description=item.description||'Paquete Fluthin validado.',stars=Number.isInteger(item.stars)?`${item.stars} estrellas GitHub`:'Estrellas GitHub no disponibles',targets=(item.platformTargets||[]).map(target=>`<span class="foundstore-creator-target">${escapeHtml(target)}</span>`).join('');
    return `<a class="foundstore-creator-package" href="${packagePath(item)}" aria-label="Abrir ficha de ${escapeHtml(name)}"><div class="foundstore-creator-art">${media(visuals.portrait||visuals.splash||visuals.banner,visuals.icon,`${name} — portada`, 'portrait')}<span class="foundstore-creator-art-fallback">${initial}</span></div><div class="foundstore-creator-copy"><div class="foundstore-creator-identity"><div class="foundstore-creator-icon">${media(visuals.icon,'',`${name} — icono`,'icon')}<span>${initial}</span></div><div><p class="foundstore-package-category">${escapeHtml(publisher)}</p><h3>${escapeHtml(name)}</h3><p class="foundstore-creator-author">@${escapeHtml(author)}</p></div></div><p class="foundstore-creator-summary">${escapeHtml(description)}</p><div class="foundstore-creator-meta"><span class="foundstore-package-stars"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="m12 2 3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2Z"/></svg>${escapeHtml(stars)}</span>${targets?`<span class="foundstore-creator-targets">${targets}</span>`:''}</div></div></a>`;
  };
  window.FoundstoreUI={escapeHtml,packageCard,creatorPackageRow,packagePath};
}(window));
