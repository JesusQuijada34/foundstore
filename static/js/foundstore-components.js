(function attachFoundstoreComponents(window){
  const escapeHtml=value=>String(value??'').replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));
  const packagePath=item=>`/${encodeURIComponent(item.author||item.owner||'')}/${encodeURIComponent(item.slug||'')}`;
  const media=(source,fallback,alt,kind)=>source?`<img src="${escapeHtml(source)}" ${fallback?`data-fallback="${escapeHtml(fallback)}"`:''} alt="${escapeHtml(alt)}" loading="lazy" onerror="if(this.dataset.fallback){this.src=this.dataset.fallback;this.dataset.fallback=''}else{this.hidden=true}">`:'';
  const packageCard=(item,options={})=>{
    const visuals=item.visuals||{},name=item.name||item.slug||'Paquete Fluthin',initial=escapeHtml(name.slice(0,1).toUpperCase()||'?'),author=item.author||item.owner||'No informado',publisher=item.publisher||item.company||author,stars=Number.isInteger(item.stars)?`${item.stars} estrellas GitHub`:'Estrellas GitHub no disponibles',targets=(item.platformTargets||[]).map(target=>`<span class="foundstore-package-target">${escapeHtml(target)}</span>`).join(''),extraClass=options.newCard?' new-card':'';
    return `<a class="foundstore-package-card${extraClass}" href="${packagePath(item)}" aria-label="Abrir ficha de ${escapeHtml(name)}"><div class="foundstore-package-banner">${media(visuals.splash||visuals.banner,visuals.icon,`${name} — portada`,'banner')}<span class="foundstore-package-banner-fallback">${initial}</span><div class="foundstore-package-icon">${media(visuals.icon,'',`${name} — icono`,'icon')}<span class="foundstore-package-icon-fallback">${initial}</span></div></div><div class="foundstore-package-body"><h3 class="foundstore-package-name">${escapeHtml(name)}</h3><p class="foundstore-package-fact"><b>Empresa</b>${escapeHtml(publisher)}</p><p class="foundstore-package-fact"><b>Creador</b>${escapeHtml(author)}</p><span class="foundstore-package-stars"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="m12 2 3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2Z"/></svg>${escapeHtml(stars)}</span></div>${targets?`<div class="foundstore-package-targets">${targets}</div>`:''}</a>`;
  };
  window.FoundstoreUI={escapeHtml,packageCard,packagePath};
}(window));
