(function attachFoundstoreComponents(window){
  const escapeHtml=value=>String(value??'').replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));
  const packagePath=item=>`/${encodeURIComponent(item.author||item.owner||'')}/${encodeURIComponent(item.slug||'')}`;
  const media=(source,fallback,alt)=>source?`<img src="${escapeHtml(source)}" ${fallback?`data-fallback="${escapeHtml(fallback)}"`:''} alt="${escapeHtml(alt)}" loading="lazy" decoding="async" onerror="if(this.dataset.fallback){this.src=this.dataset.fallback;this.dataset.fallback=''}else{this.hidden=true}">`:'';
  const starIcon='<svg class="foundstore-stars-icon" viewBox="0 0 24 24" aria-hidden="true"><path d="m12 2 3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27 12 2Z"/></svg>';
  const labelsFor=options=>({badge:'Selección del creador',featured:'Destacado',publisher:'Publicador',stars:'estrellas GitHub',noStars:'Sin valoración GitHub',open:'Abrir ficha',...(options.labels||{})});
  const compactStoreCard=(item,options={})=>{
    const visuals=item.visuals||{},icon=item.packageIcon||visuals.icon,name=item.name||item.slug||'Paquete Fluthin',initial=escapeHtml(name.slice(0,1).toUpperCase()||'?'),author=item.author||item.owner||'No informado',publisher=item.publisher||item.company||author,labels=labelsFor(options),stars=Number.isInteger(item.stars)?`${item.stars} ${labels.stars}`:labels.noStars;
    return `<a class="foundstore-store-card${options.newCard?' new-card':''}" href="${packagePath(item)}" aria-label="${escapeHtml(labels.open)}: ${escapeHtml(name)}"><div class="foundstore-store-card-icon">${media(icon,'',`${name} — icono`)}<span class="foundstore-store-card-fallback" aria-hidden="true">${initial}</span></div><div class="foundstore-store-card-body"><span class="foundstore-store-card-badge">${escapeHtml(item.featured?labels.featured:labels.badge)}</span><h3 class="foundstore-store-card-name" title="${escapeHtml(name)}">${escapeHtml(name)}</h3><p class="foundstore-store-card-publisher" title="${escapeHtml(publisher)}">${escapeHtml(publisher)}</p><span class="foundstore-store-card-stars">${starIcon}<span>${escapeHtml(stars)}</span></span></div></a>`;
  };
  const packageCard=(item,options={})=>{
    const visuals=item.visuals||{},icon=item.packageIcon||visuals.icon,name=item.name||item.slug||'Paquete Fluthin',initial=escapeHtml(name.slice(0,1).toUpperCase()||'?'),author=item.author||item.owner||'No informado',publisher=item.publisher||item.company||author,category=item.category||'Aplicación Fluthin',description=item.description||'Paquete Fluthin validado.',stars=Number.isInteger(item.stars)?`${item.stars} estrellas GitHub`:'Estrellas GitHub no disponibles',targets=(item.platformTargets||[]).map(target=>`<span class="foundstore-package-target">${escapeHtml(target)}</span>`).join(''),extraClass=options.newCard?' new-card':'';
    return `<a class="foundstore-package-card${extraClass}" href="${packagePath(item)}" aria-label="Abrir ficha de ${escapeHtml(name)}"><div class="foundstore-package-main"><div class="foundstore-package-icon">${media(icon,'',`${name} — icono`,'icon')}<span class="foundstore-package-icon-fallback">${initial}</span></div><div class="foundstore-package-body"><p class="foundstore-package-category">${escapeHtml(category)}</p><h3 class="foundstore-package-name">${escapeHtml(name)}</h3><p class="foundstore-package-summary">${escapeHtml(description)}</p><p class="foundstore-package-fact"><b>Empresa</b>${escapeHtml(publisher)} <span aria-hidden="true">·</span> <b>Creador</b>${escapeHtml(author)}</p><span class="foundstore-package-stars"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="m12 2 3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2Z"/></svg>${escapeHtml(stars)}</span></div></div><div class="foundstore-package-banner">${media(visuals.splash||visuals.banner,visuals.icon,`${name} — vista previa`,'banner')}<span class="foundstore-package-banner-fallback">${initial}</span></div>${targets?`<div class="foundstore-package-targets">${targets}</div>`:''}</a>`;
  };
  const creatorPackageRow=(item,options={})=>compactStoreCard(item,options);
  window.FoundstoreUI={escapeHtml,packageCard,creatorPackageRow,compactStoreCard,packagePath};
}(window));
